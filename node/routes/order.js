'use strict'

var util = require('util')
var utils = require('../utils')

var format = util.format
var logger = utils.getLogger()

exports = module.exports

exports.init = function (server) {
	logger.info('init order apis.')
}

var order = {
	
}